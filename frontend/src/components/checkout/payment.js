import React from "react"
import { styled } from '@mui/material/styles'

const Payment = () => {
    return (
        <PaymentSection id="PaymentSection">
        
        </PaymentSection>
    )
}

export default Payment

const PaymentSection = styled('div')(({ theme }) => ({
    
}))